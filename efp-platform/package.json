{
  "name": "efp-platform",
  "version": "1.0.0",
  "private": true,
  "description": "EFP Football Relationship Intelligence Platform — regression test runner",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0"
  }
}
